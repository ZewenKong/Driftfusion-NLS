% README.m - solid state physics transport model

% - - - - - - - - - - Scripts

%% cation density v.s. normalised mobility
var_cat;

%% lattice constant v.s. normalised mobility
var_lat; % 

%% active layer thickness v.s. normalised mobility
var_thickness;

%% cation_density / lattice constant v.s. normalised mobility
contour_cat_lat;

% - - - - - - - - - - Function

% maxFatIF % - - -> max electric field at interface
% maxFwithinRPP % - - -> max electric field within RPP layer
% normdmu % - - -> normalised mobility calculation (with max field input)